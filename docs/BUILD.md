# Build and Development Environment

The initial target environment is seL4 + Microkit running under QEMU on the host, with a small terminal shim on the host side.

Detailed reproducible build instructions will be added once the first milestone environment is stable.

For now the focus is on defining the exact minimal surface of the containment vector and the host protocol before writing large amounts of C.
